{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "ls -la && cd ayursutra-react && npm install && VITE_API_URL= npm run build && cd ../ayursutra-backend && npm install"
  },
  "deploy": {
    "startCommand": "cd ayursutra-backend && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
